import React from 'react';
import { Splash } from '@templates';

export const Home = () => (
    <Splash
        title='Bill Manager'
        subtitle='A tool created to easily manage paying bills and keeping historic data about paying bills'
    />
);

export default Home;
